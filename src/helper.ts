import * as WellPingTypes from "wellping/src/helpers/types";

export function getWellPingStudyFileJSONFromEditorJSON(studyFile: any): any {
  console.warn(__DEV__);

  const myTest: WellPingTypes.StudyID = "test";
  console.warn(myTest);

  return myTest;
}
